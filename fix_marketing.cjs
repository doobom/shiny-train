const fs = require('fs');
let code = fs.readFileSync('src/components/admin/AdminMarketing.tsx', 'utf8');

// I will just download the original AdminMarketing.tsx from the github repo? I don't have internet.
// I will just cut the file to only include the last instance of the component? No, all instances are corrupted.
